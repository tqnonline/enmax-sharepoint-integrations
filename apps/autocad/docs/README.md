# Docs

Architecture notes and decision records.

Full spec docs and implementation plans live on the `specs` orphan branch:

```powershell
git worktree add .worktrees/specs specs
# Read: .worktrees/specs/docs/superpowers/
```
